GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant
PEXELS_API_KEY=
# Optional: set to 1 to use OpenStreetMap Overpass + Groq merge (live). Omit or 0 = AI-only curation (default).
# TRIPSYNC_USE_LIVE_OVERPASS=1
