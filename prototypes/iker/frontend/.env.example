# Orbit Together — frontend env (optional overrides)
# Copy to .env if you need to change defaults.

# Where the backend lives (default: same origin with /api proxy).
# Only set this if you're hosting the API elsewhere.
VITE_API_BASE_URL=
