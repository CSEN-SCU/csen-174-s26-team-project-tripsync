# LLM-Integration package — used by `dart test` / CI when passing a map to LlmConfig.fromEnvironment.
# Flutter app uses the same variable names via --dart-define-from-file from `app/.env`.

GROQ_API_KEY=
GROQ_LLM_MODEL=llama-3.3-70b-versatile
GROQ_LLM_BASE_URL=https://api.groq.com/openai/v1
